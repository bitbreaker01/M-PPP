# PENDIENTES — documento temporal

Se limpia a medida que se decide. **Cuando quede vacío, se elimina.**

Última actualización: 2026-09-20. Diseño `00` a `07` revisado por el aprobador; queda abierta una sola decisión de diseño: los idiomas (D-3).

## 0. Diseño que falta (bloquea construir esas partes, no el modelo de datos)

| # | Qué falta | Qué necesita |
|---|---|---|
| D-1 | **La app model-driven** (`05-app-model-driven.md`): sitemap, vistas por perfil, formularios, con qué gesto se dispara cada transición, cómo ve el supervisor la solicitud original. | Respuestas del aprobador a seis preguntas: unidad de trabajo del ejecutivo (bandeja de filas o por solicitud); acciones masivas sobre varias filas; qué necesita ver el supervisor; bandeja de no reconocidos y si hace falta el cuerpo del correo en texto; carga de catálogos uno a uno o por importación; avisos de trabajo nuevo. |
| D-2 | **Los cuatro flujos** (`07-flujos.md`): disparadores, acciones, expresiones, errores, reintentos, concurrencia. | Lo diseña el arquitecto; necesita del aprobador el nombre del buzón y cómo se envían las respuestas (desde el buzón dedicado, como respuesta al hilo o como correo nuevo). |

## 00. Construcción: DETENIDA por orden del aprobador (2026-09-20)

No se construye ningún componente hasta que el aprobador revise el diseño de la app (`05`) y de los flujos (`07`) y dé la orden. **Dev está vacío**: existe solo la solución `sanic_mppp_sol_mantenimientoppp`, sin componentes (el choice de prueba del ciclo 1 se borró el 2026-09-20 por orden del aprobador).

Las dos observaciones del revisor sobre `herramientas/construir/choice_global.py` quedaron **resueltas el 2026-09-20** (estado `error` frente a `bloqueado`, red de contención sin red), junto con lo que destaparon: toda respuesta del entorno se valida por tipo hasta el último nivel con `exigir_forma` (`_comun.py`). 105 pruebas sin red, comprobadas por mutación.

## 0. Diseño en revisión

| # | Documento | Qué necesita del aprobador |
|---|---|---|
| D-1 | `05-app-model-driven.md` | **Cerrado el 2026-09-20.** Decisiones DA-01 a DA-09 aprobadas y mockup revisado por el aprobador: pidió la navegación entre Cliente, Plan y correo Autorizado (ya está en el mockup y en `05`); "aparte de eso, lo miro todo bien". |
| D-2 | `07-flujos.md` | **Cerrado el 2026-09-20.** Flujos aprobados; el aprobador confirmó que el plugin liviano resuelva también `REMITENTE_RECONOCIDO`. |
| D-3 | **Idiomas** | El aprobador exige (2026-09-20) que todo nazca **listo para varios idiomas**. Regla (verificada en Learn y en Dev): las etiquetas de **metadatos** (tablas, columnas, choices, vistas, formularios) van en 1033 y la plataforma las usa en cualquier idioma base que no tenga las suyas; las etiquetas **embebidas** en el XML del componente (títulos del sitemap, textos de comandos y lo que cada export real demuestre) llevan **un título por idioma** de la lista del proyecto, 1033 y 3082 para empezar. **No hace falta habilitar español en Dev**: el aprobador ya lo hizo a mano desde un entorno en inglés. Falta: pasarlo a `01` §0 y al formato del playbook (`idiomas`), y probar temprano una importación en un entorno con base español. |
| D-4 | `02` §2.6, tabla Regla | **Cerrado el 2026-09-20.** La primaria de Regla es autonumérica y opcional, `REG-{SEQNUM:4}` (`02` DD-20). |
| D-5 | `02` §3.3 y §3.4 | **Cerrado el 2026-09-20.** Son opcionales a propósito, y además autonuméricas: `RES-{SEQNUM:10}` y `BIT-{SEQNUM:10}` (`02` DD-20). |
| D-6 | Relaciones 4.10 y 4.11 (BP-PP-188) | **Cerrado el 2026-09-20.** Aprobado: con más de una relación entre el mismo par de tablas, cada una lleva como sufijo el nombre de su lookup sin prefijo. Agregado a BP-PP-188 y a `01` §2. |
| D-7 | `02` §3.3 y §3.4 | **El lookup `sanic_solicitudid` de Resultado de regla y de Bitácora se construye opcional**, como dice el diccionario y como figuraba en la lista de relaciones que el aprobador aprobó el 2026-09-20. Queda la duda de si conviene requerido (son relaciones parentales); se cambia después sin costo. No frena nada. **2026-09-21**: el aprobador confirmó que la clave 5.10 se construye con el lookup opcional, tal como está; en la práctica nunca queda vacío porque el plugin siempre carga la solicitud. |
| D-8 | `04` §4 | **CERRADA (aprobador, 2026-09-21): por equipo.** El administrador crea un grupo de seguridad en Entra ID y su equipo de grupo en Power Platform, y a ese equipo se le asigna el perfil. Un perfil de solo lectura en fase 1 (`CSP - MPPP - Datos sensibles`); el de lectura y creación nace en fase 3. Pasado a `04` §4 e inventario 13.7. |
| D-9 | `03` y `04` §3 | **CERRADA (aprobador, 2026-09-21): `ExecutePrivilegeName = prvCreatesanic_mppp_tbl_solicitud`** para las dos Custom API de fase 1. Las de fase 2 se deciden al diseñar esa fase. Pasado a `03` (encabezado). |
| D-10 | `01` §7 (BP-PP-197) | **CERRADA (aprobador, 2026-09-21): se aceptan como excepción documentada, y no se repite.** Son siete nombres que ya estaban construidos cuando se decidió BP-PP-197 y que el Web API no deja cambiar: las 6 claves alternativas con tilde (`… Plan - Código`, `… Autorización - Autorizado y plan`, `… Parámetro - Nombre y versión`, `… Regla - Código`, `… Fila - Solicitud y número de fila`, `… Resultado de regla - Solicitud y código de regla`) y el nombre interno del sí/no `solicitud.sanic_requiererevision` ("Requiere revisión"). **Cómo se garantiza que no se repita**: las seis herramientas rechazan, antes de tocar el entorno, cualquier nombre visible con un carácter fuera de ASCII; las siete excepciones están en `herramientas/construir/excepciones_nombres.json` con su texto exacto, y una prueba fija esa lista (agregar una octava rompe la prueba, y cambiarla obliga a pasar por el aprobador); `tabla.py` ahora compara también el nombre interno de cada sí/no, así que renombrar a futuro una columna sí/no se ve como `difiere` en vez de pasar en silencio. |
| D-11 | `04` §2 (D-25) y `03` §4 | **CERRADA (aprobador, 2026-09-21): falla cerrado.** Una identidad que trae más de un rol entre Ejecutivo, Supervisor y RPA no puede hacer ninguna transición de Fila. Generaliza D-25. Pasado a `04` §2 y fijado en las pruebas de aceptación de 7.1 Dominio. |
| D-12 | `03` §7, LP-04 y LP-05 | **CERRADA (aprobador, 2026-09-21): se confirma la lectura literal de LP-04.** El orden se exige en todo lo que se llega a recorrer (una fila o una celda que retrocede o se repite invalida el archivo); un salto EN ORDEN más allá de la ventana corta la lectura sin invalidar nada (fila: termina la lectura; celda: termina esa fila y los campos que faltan quedan vacíos); lo que venga después del corte no se mira. Pasado a `03` §7 (nota bajo la tabla) y fijado en `LectorVentanaYOrdenAceptacion.cs`. |
| D-13 | `02` §2.6 y `03` §1 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** falla cerrado y ruidoso en los dos casos (`ConfiguracionDeReglasInvalidaException`); además 7.10 impide desactivar una regla de la que dependen otras activas. *Planteo original:* **Dos casos de mala configuración del catálogo de reglas, sin comportamiento definido.** (a) Una regla ACTIVA cuyo código no tiene evaluador en el C# (`02` §2.6 solo dice que "es un error de configuración"). (b) Una regla activa que depende de otra que está INACTIVA (el guardado valida que el código exista, no que esté activa). **Recomendación para las dos: falla cerrado y ruidoso**: la validación lanza un error, la transacción se revierte, la Solicitud sigue en Ingresada, `MPPP-VIG` reintenta y al agotar los reintentos marca `sanic_requiererevision`. Alternativas: tratar la regla como Omitida (silencioso: la validación se debilita sin que nadie se entere) o, en (b), ignorar la dependencia y evaluar igual (el evaluador puede reventar porque le falta lo que su dependencia garantizaba). Para (b) conviene además que el plugin de guardado de Regla (7.10) impida desactivar una regla de la que dependen otras activas. |
| D-14 | `03` §1 paso 5 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** Sin autorización gana sobre Rechazada en validación (el mensaje lleva igual todos los motivos); `AUTORIZACION_CORREO_PLAN` no admite otro efecto que Rechaza (lo impide 7.10 y el motor falla cerrado si igual llega). *Planteo original:* **Una fila que falla `AUTORIZACION_CORREO_PLAN` Y ADEMÁS otra regla: ¿queda Sin autorización o Rechazada en validación?** El diseño da las dos frases en ese orden y no dice qué pasa si coinciden (puede pasar: la autorización depende solo de `PLAN_EXISTE`, así que una fila con el banco mal escrito y sin autorización falla las dos). **Recomendación: Sin autorización** (es lo que implementa el motor hoy, por orden literal): es la causa más grave y la que cambia qué se le dice al cliente; `sanic_mensaje` lleva igual todos los motivos. Alternativa: Rechazada en validación, con el argumento de que aunque se autorice igual no se podría procesar. **Y un cruce que encontró la revisión de código**: si alguien pone `AUTORIZACION_CORREO_PLAN` en efecto "Advierte" en el catálogo, hoy la fila queda Sin autorización igual (lectura literal: "falla `AUTORIZACION_CORREO_PLAN` → Sin autorización"), mientras que cualquier otra regla en "Advierte" no rechaza. **Recomendación: que esa regla NO admita otro efecto que Rechaza**: lo impide el plugin de guardado de Regla (7.10), y el motor sigue fallando cerrado si igual le llega. Una autorización que solo "advierte" es dejar pasar gestiones de quien no está autorizado. |
| D-15 | `02` §2.6 y `07` DF-01 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** columna entera nueva `sanic_cantidadexcel` en Solicitud, que llena `MPPP-ING`. `TRAE_ADJUNTO` = adjuntos ≥ 1; `ADJUNTO_ES_EXCEL` = excel ≥ 1; `UN_SOLO_EXCEL` = excel = 1. Pendiente de construir: capacidad de agregar columna en `tabla.py` (con ensayo ZZ) y la columna en Dev. *Planteo original:* **`ADJUNTO_ES_EXCEL` y `UN_SOLO_EXCEL` no se pueden distinguir con los datos que el plugin recibe.** `MPPP-ING` guarda `sanic_cantidadadjuntos` (todos los adjuntos) y sube el Excel a `sanic_exceloriginal` solo si hay exactamente uno. Con eso, "mandó un PDF", "mandó dos Excel" y "mandó tres archivos y ninguno es Excel" se ven iguales: hay adjuntos y no hay Excel. **Recomendación: una columna entera nueva en Solicitud, `sanic_cantidadexcel`**, que llena `MPPP-ING`; las reglas quedan: `TRAE_ADJUNTO` = adjuntos ≥ 1; `ADJUNTO_ES_EXCEL` = excel ≥ 1; `UN_SOLO_EXCEL` = excel = 1. Permite decirle al cliente exactamente qué hizo mal. Costo: una columna más en una tabla ya construida (hay que darle a `tabla.py` la capacidad de agregar una columna, con su ensayo). Alternativa sin columna: fundir las dos reglas en una (`ADJUNTO_EXCEL_UNICO`) con un mensaje genérico ("adjunte un único archivo de Excel"). |
| D-16 | `02` §2.6 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** las cinco condiciones quedan como se propusieron. *Planteo original:* **Las condiciones exactas de las 5 reglas del sobre no están escritas** (solo su nombre y su cadena de dependencias). **Propuesta**: `TRAE_ADJUNTO`: `sanic_cantidadadjuntos` ≥ 1 · `ADJUNTO_ES_EXCEL` y `UN_SOLO_EXCEL`: según D-15 · `ESTRUCTURA_PLANTILLA`: el lector de 7.2 devuelve válido (hoja, encabezados y orden correctos; si no, la razón son los errores del lector, que ya son texto para el cliente) · `TIENE_FILAS`: el lector devolvió al menos una fila con datos. |
| D-17 | `03` §1 paso 5, DD-10 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** referencia = código de banco (3) + cuenta rellena con ceros A LA IZQUIERDA hasta 17 = 20 exactos; no se cumple si la cuenta tiene más de 17 caracteres, si trae algo que no sea dígitos, o si el banco no tiene código de 3 dígitos. El aprobador aceptó la recomendación completa (relleno a la izquierda, solo dígitos). *Planteo original:* **Fórmula exacta de la referencia de formato 11, y cuándo falla `REFERENCIA_FORMATO_11`.** El diseño dice "código de banco de 3 dígitos + ceros + número de cuenta hasta 20 caracteres". **Propuesta**: referencia = código de banco (3) + número de cuenta relleno con ceros A LA IZQUIERDA hasta 17 = 20 caracteres exactos. **No se cumple** (y la fila se rechaza) si la cuenta tiene más de 17 caracteres, si trae algo que no sea dígitos, o si el banco no tiene código de 3 dígitos en `plantilla.listas`. En formatos 06 y 10 siempre se cumple (no deriva nada, DD-15). Necesito que confirmes el lado del relleno y si la cuenta puede traer letras. |
| D-18 | `02` §2.5 (`plantilla.listas`, `plantilla.estructura`) y DD-01 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** (a) comparación sin mayúsculas, tildes ni espacios a los lados, con variantes explícitas; (b) `formato` cerrado: `texto`, `digitos`, `alfanumerico` + `largoMinimo`/`largoMaximo`, sin expresiones regulares; (c) `plantilla.obligatoriedad` como lista de excepciones sobre un valor por defecto. *Planteo original:* **Tres formas que el diseño nombra y no define.** (a) **Cómo se compara un valor contra `plantilla.listas`**: propongo sin distinguir mayúsculas, tildes ni espacios al inicio y al final, y que las "variantes de escritura" sean explícitas en el parámetro: `{"gestion":[{"valor":"Inclusion","variantes":["inclusión","alta"]}]}`. (b) **El `formato` de cada campo en `plantilla.estructura`** (lo usa `LARGOS_Y_FORMATO`): propongo una lista corta y cerrada: `texto`, `digitos`, `alfanumerico`, más `largoMinimo` y `largoMaximo`; nada de expresiones regulares en un parámetro (una mal escrita tumba la validación entera). (c) **Forma de `plantilla.obligatoriedad`**: propongo una lista de excepciones sobre un valor por defecto: `{"porDefecto":"obligatorio","opcionales":[{"campo":"referencia"}],"reglas":[{"gestion":"Exclusion","clasificacion":"*","formato":"*","opcionales":["moneda","banco"]}]}`; arranca con todo obligatorio salvo Referencia (DD-16) y crece cuando escribas la matriz. |
| D-19 | `02` §2.6 (`sanic_mensajecliente`) y `03` §1 | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** `sanic_mensajecliente` es una plantilla con marcadores (`{valor}`, `{campo}`, `{plan}`, `{regla}`); con el texto vacío el evaluador usa uno propio por defecto. *Planteo original:* **Cómo conviven el texto fijo del catálogo con los mensajes que llevan valores** ("el valor recibido se cita en el mensaje"; "no está autorizado sobre el plan X"). **Propuesta**: `sanic_mensajecliente` es una PLANTILLA con marcadores que el evaluador completa (`{valor}`, `{campo}`, `{plan}`, `{regla}`); si la regla tiene más de un caso (la de autorización tiene dos), el texto del catálogo cubre el caso general y el evaluador agrega la precisión. Si el texto del catálogo está vacío, el evaluador usa uno propio por defecto, para que nunca salga un motivo en blanco. Así el negocio puede cambiar la redacción sin tocar código, y el código nunca depende de que la redacción exista. |
| D-20 | `02` §2.6, columna `sanic_efecto` | **CERRADA (aprobador, 2026-09-21): aprobada la recomendación.** "Envía a revisión" es válido en las reglas del sobre (niveles Correo y Solicitud) y nunca en Registro; 7.10 lo impide y el motor lo trata como catálogo mal armado. *Planteo original:* El diccionario dice dos veces que el efecto "Envía a revisión" "solo es válido en el nivel Solicitud", pero las dos reglas de nivel Correo (`ES_CORREO_NUEVO`, `REMITENTE_RECONOCIDO`) lo usan. **Lectura propuesta**: es válido en las reglas DEL SOBRE (niveles Correo y Solicitud) y nunca en las de Registro. Corrijo la redacción del diccionario cuando lo confirmes. **Y su consecuencia en el motor** (revisión de código): una regla de nivel Registro con efecto "Envía a revisión" que no se cumple hoy no rechaza la fila ni hace nada (su motivo sí sale en el mensaje). **Recomendación**: el plugin de guardado de Regla (7.10) lo impide, y si igual llega, el motor lo trata como catálogo mal armado (falla cerrado), igual que D-13. |
| D-39 | `02` §1 (Efecto de regla) y `03` §1 paso 7 | **CERRADA (aprobador, 2026-09-21): gana la revisión.** Si falla una regla del sobre con "Envía a revisión", la Solicitud queda No reconocida aunque además otra regla rechace. Pasado a `03` §1 pasos 4 y 7. *Planteo original:* (Se numera 39 para no chocar con las D-21 a D-38 de `03` y `04`.) **Una regla del sobre con efecto "Envía a revisión" que falla, evaluada por la Custom API de validación.** El diccionario dice qué significa (no se responde, queda No reconocida, la revisa un ejecutivo) pero `03` §1 paso 7 solo habla de Rechazada y En proceso, y el código lo ignoraba. Hoy ninguna semilla de nivel Solicitud usa ese efecto, así que es un caso de catálogo futuro. **Lectura fijada en las pruebas (la conservadora)**: si falla una regla del sobre con "Envía a revisión", la Solicitud queda **No reconocida**, haya o no filas validadas; y si ADEMÁS falla otra con "Rechaza", **gana la revisión**: no se le responde al cliente sin que lo mire una persona. Alternativa: que gane el rechazo (el cliente recibe respuesta más rápido, pero una regla que alguien configuró para que lo mire un humano queda sin efecto). A confirmar; cambiarlo es una línea y una prueba. |
| D-40 | `03` §1 paso 5, `OBLIGATORIEDAD` y DD-10 | **CERRADA (aprobador, 2026-09-21): se queda en la matriz.** Nada de obligatoriedad va fijo en el código: que la Referencia sea opcional en formato 11 lo dice `plantilla.obligatoriedad`, como todo lo demás. El código no cambia. Queda como nota para quien cargue la matriz: en formato 11 la Referencia recibida no se usa, así que conviene dejarla siempre opcional. *Planteo original:* (Revisión de código, 2026-09-21.) En un plan formato 11 la Referencia recibida no se usa para nada (rige la construida, DD-10), pero que sea opcional depende de que el administrador la deje en los `opcionales` de `plantilla.obligatoriedad`. Si un día la saca por error, toda fila de formato 11 con la celda Referencia vacía se rechaza por un dato que ese formato ni mira. **Recomendación: que `OBLIGATORIEDAD` trate la Referencia como SIEMPRE opcional cuando el plan es formato 11**, diga lo que diga el parámetro. Alternativa: dejarlo como está y confiar en el parámetro. |
| D-41 | DD-08 y DD-01 | **CERRADA (aprobador, 2026-09-21): menos detalle.** Los mensajes por defecto NO citan el valor recibido: nombran el campo ("Uno o más valores no son válidos: Banco"). El mensaje es por fila, así que el cliente ubica la celda sin el valor. DD-01 y `03` §1 paso 5 se corrigen: la columna queda vacía y el mensaje nombra el campo. El marcador `{valor}` sigue disponible para la redacción del catálogo. *Planteo original:* (Revisión de código, 2026-09-21.) Ningún mensaje cita la cuenta ni la identificación, pero DD-01 obliga a citar el valor recibido en las listas y en el plan. Si el cliente escribe por error su número de cuenta en la celda de Banco o de No. Plan, ese número vuelve citado en el acuse (hasta 40 caracteres). **Recomendación: que `MensajeAlCliente.Citar` enmascare toda corrida de 6 o más dígitos seguidos** (deja los dos primeros y los dos últimos: `12••••89`): ningún valor legítimo de lista ni de plan tiene 6 dígitos seguidos, así que no se pierde nada, y una cuenta o una identificación tipeada en la celda equivocada no viaja por correo. Alternativa: aceptarlo como riesgo (el dato vuelve al mismo cliente que lo escribió). |
| D-42 | E-17, `02` §2.4, `03` §0 y §1 | **CERRADA (aprobador, 2026-09-21): la evidencia no cuenta para la vigencia.** Una autorización vale con el Autorizado, el Plan, el Cliente y la propia AutorizacionPlan activos, tenga o no documento firmado. Razón del aprobador: no se afecta al cliente porque alguien del banco no cargó el documento. La vista "Autorizaciones sin evidencia" y la marca en las subgrillas quedan como lista de pendientes del administrador. Cambia la definición única de "vigente" para `REMITENTE_RECONOCIDO` y `AUTORIZACION_CORREO_PLAN` (un solo mensaje), y E-17 queda corregida. También: el texto de `REFERENCIA_FORMATO_11` no menciona el largo ("hasta 17"). |

| D-43 | `03` §4 "Cierre" | **ABIERTA, no frena la fase 1.** (Revisión de código, 2026-09-21.) Dos Filas de la misma Solicitud que terminan casi al mismo tiempo disparan dos veces el step de cierre, en transacciones separadas: cada una puede leer a la otra como todavía abierta y **ninguna** pasar la Solicitud a Procesada, que quedaría En proceso para siempre (el cliente no recibe su respuesta final). `MPPP-VIG` hoy no barre ese caso. **Recomendación: que `MPPP-VIG` incluya un barrido de "En proceso sin ninguna fila abierta" y dispare el cierre.** Es el mismo tipo de riesgo residual que ya se aceptó en `03` §3 para AS400, pero acá hay una salida barata. Alternativas: concurrencia optimista sobre la Solicitud, o releer y reintentar dentro del step. |
| D-44 | `04` §3 y `03` §4 | **ABIERTA, no frena la fase 1.** El step de transición tiene que reconocer el rol del **usuario de aplicación del RPA**, que es de fase 2 y todavía no existe: hoy el código usa el nombre provisorio `sr_mppp_rpa`. Al construir la fase 2 hay que crear ese rol con ese nombre exacto, o cambiar la constante. Mientras tanto ninguna transición del RPA es posible, que es lo correcto en fase 1. |
| D-45 | `02` §Parámetros y `06` 9.1 | **ABIERTA, FRENA la carga de 9.1.** (Relevamiento del 2026-09-22.) De los 12 parámetros semilla, **5 no tienen valor inicial en ningún archivo del repo**: `retencion.dias.general`, `retencion.dias.noreconocidas`, `vigilancia.minutos.sinvalidar`, `vigilancia.minutos.sinresponder`, `vigilancia.reintentos.maximo`. Los usa `MPPP-VIG` (`07` §101-103) y el reintento de `03` §69. Además hay una **contradicción**: `02` marca los dos `retencion.dias.*` como **fase 3**, pero `06` 9.1 los lista como semilla de fase 1. Hay que decidir el número de los tres de vigilancia y si los dos de retención entran ahora o en fase 3. |
| D-46 | `02` §Parámetros y `06` 9.1 | **ABIERTA, FRENA la carga de 9.1.** `plantilla.estructura` y `plantilla.listas` **no existen como JSON cargable** en `diseno/`: solo están su forma (D-18) y largos de 4 de los 10 campos. Falta el mapeo completo columna↔campo↔encabezado y la lista de valores con sus variantes y los códigos de banco. **La fuente buena está en el repo y ningún documento de diseño la cita**: `datos/plantilla/Inclusiones_Exclusiones en PPP.xlsx`, hoja `Listas` (bancos con su código real: LAFISE 006, BAC 007, BANPRO 008, FICOHSA 009, BDF 012, AVANZ 019, PRODUZCAMOS 023, ATLANTIDA 037) y hoja `Datos` (encabezado en la fila 12; B=Gestion, C=Clasificacion, D=No. Plan, E=Nombre, F=Tipo ID, G=No. Identificacion, H=Referencia, I=No. Cuenta, J=Moneda, K=Banco). **Hay que armar los dos JSON contra ese archivo y que los apruebe el aprobador.** |
| D-47 | `02` §Reglas y `06` 9.2/9.3 | **ABIERTA, no frena si se acepta la derivación.** Las 15 reglas tienen todo su contenido en `02` menos `sanic_orden`: el diseño solo fija el orden RELATIVO por dependencia, y el motor exige que quien depende tenga un `Orden` estrictamente mayor. **Propuesta: derivar los números por orden topológico en múltiplos de 10** (10, 20, 30…), documentando la derivación. `sanic_orden` es un dato mutable: cambiarlo después no cuesta nada. Ojo: en nivel Registro el orden también decide en qué secuencia ve el cliente los motivos de rechazo de una fila (`EstadosPorReglas.MotivosDeLaFila`), así que tiene una arista de negocio. |

| D-48 | `04` §2 | **ABIERTA, no frena nada: es deuda de documentación.** (Verificación de accesos, 2026-09-23.) La matriz de `04` §2 **no lista `sanic_mppp_tbl_motivoaccion`**, la tabla técnica del diálogo de motivo (`05` DA-03). Los roles Ejecutivo y Supervisor sí la traen, con `C R W D` en alcance **usuario** — está en sus playbooks y es correcto: es un registro descartable y cada quien toca solo el suyo. Pero como la matriz es la fuente contra la que se verifica, una tabla que no está ahí se lee como privilegio de más. Hay que agregar la fila (y decidir si `D` en alcance usuario contradice el "Nadie borra" de §2, o si ese principio se declara solo sobre las tablas de negocio). Mientras tanto la excepción vive anotada en `herramientas/pruebas/verificar_accesos.py`. |
| D-49 | `04` §4 e inventario 13.7 | **ABIERTA, FRENA las pruebas de digitación.** (Verificación de accesos, 2026-09-23.) `sanic_numeroidentificacion` y `sanic_numerocuenta` están protegidas de verdad (`IsSecured = true` las dos) y **`CSP - MPPP - Datos sensibles` no tiene ni un usuario ni un equipo asignado**: hoy solo las leen los System Administrator, por el perfil automático de la plataforma. Es decir que **ningún Ejecutivo ni Supervisor ve lo que tiene que digitar**. El perfil viaja en la solución (6.1); el grupo de Entra, el equipo de grupo y la asociación equipo ↔ perfil **no viajan** (13.7), y en Dev nunca se hicieron. **PARCHEADO el 2026-09-23 por orden del aprobador**: `User1` y `User2` quedaron asignados **directo** al perfil, solo en Dev, para no frenar las pruebas de digitación. **Lo que queda abierto es el mecanismo de D-8**: la asociación equipo ↔ perfil sigue **sin probarse una sola vez**, y es la que va a Test y a Producción. Hay que ejercitarla antes de migrar; si falla, se descubre el día de la migración. **Y ojo con el falso positivo previo**: mientras el perfil estuvo vacío, G-05 "el Administrador de planes no ve la cuenta" pasaba… porque no la veía nadie. |

| D-50 | `04` §1 y §3 | **CERRADA el 2026-09-23 con el arreglo desplegado (1.0.9.0).** Los siete steps y las dos Custom API creaban el servicio con `CreateOrganizationService(contexto.UserId)`, cuando `04` §1 dice textual que los plugins leen y graban **con SYSTEM** (`CreateOrganizationService(null)`). Consecuencia: **ninguna persona podía transicionar una Fila** — el step de transición lee `rpa.puedeaprobar` de la tabla Parametro, sobre la que ningún rol de negocio tiene lectura, y el entorno respondía `is missing prvReadsanic_mppp_tbl_parametro`. Y una bomba a futuro: `validarsolicitud` habría fallado igual cuando existiera la cuenta de servicio real (13.2), porque `04` §3 dice que esa cuenta no lee filas, clientes, planes ni autorizados. **Por qué estuvo tres semanas escondido**: todo lo probado corrió con identidades sobre-privilegiadas (el service principal, el administrador, y los cinco flujos, cuya cuenta de servicio es provisoriamente la del administrador). **Por qué las 800 pruebas no lo veían**: el doble de `IOrganizationService` no modela privilegios. Dos de ellas, además, **fijaban el comportamiento equivocado como contrato** (`..._usa_el_usuario_del_contexto`) y hubo que corregirlas. Regresión cubierta por `herramientas/pruebas/comportamiento_roles.py` (G-00b). |
| D-51 | `04` §1 (lista blanca) | **CERRADA el 2026-09-23 con el arreglo desplegado (1.0.8.0).** La lista blanca rechazaba `modifiedby`, `modifiedon` y `modifiedonbehalfby`, que **agrega la plataforma** al `Target` en PreOperation de `Update`. El primer humano que marcó una Fila como Digitada desde la app no pudo guardar. Exceptuarlas no debilita el control: Learn las publica como columnas de solo lectura (`IsValidForCreate` y `IsValidForUpdate` en `false`), así que ningún llamador puede escribirlas. Fijado en `ReglasDeStepsAceptacion` y en `comportamiento_roles.py`. **Misma causa raíz que D-50: el control nunca se había ejecutado contra una persona.** |

| D-52 | `05` §5 (botón Revisado) | **CERRADA el 2026-09-23 con el arreglo desplegado (1.0.10.0).** El botón "Revisado" estaba a medio construir y **fallaba siempre**: pedía una nota y la escribía en `sanic_motivorevision`, que la lista blanca de `04` §1 no admite de una persona. Y aunque la hubiera admitido, esa columna **no es del operador**: la escriben `MPPP-VIG` y `MPPP-ENV` para explicar por qué hace falta revisar, y es lo que el operador LEE en *Para revisar* — la nota se lo habría comido. Encima, el plugin que `05` §5 y `pruebas/05-app-y-comandos.md` daban por hecho ("pasa la nota a la Bitácora") **no existía**, y tampoco existía un evento de Bitácora para el apagado de la marca. Resuelto como el **mockup aprobado el 2026-09-20**: el botón solo confirma, y `RevisionAtendidaStep` (PreOperation, `Update` de Solicitud, filtro `sanic_requiererevision`, orden 20, pre-imagen de la marca y del motivo) deja el motivo de la máquina en la Bitácora con actor y fecha, y limpia la columna. Evento nuevo: `Revision atendida` (159460021). **Tercer defecto del día con la misma causa raíz que D-50 y D-51: el modelo de permisos nunca se había ejercitado con una persona.** |

## A. Para confirmar con negocio, sin apuro

La matriz de obligatoriedad arranca con **todo obligatorio salvo Referencia** (DD-16). Cuando se quiera flexibilizar, se cambia el parámetro `plantilla.obligatoriedad`; no hay código que tocar. El aprobador va a escribir la versión detallada. Lo ya respondido está en `02-diccionario-datos.md`, DD-12 a DD-17.

## B. Verificaciones contra el entorno

- **PARCIALMENTE CERRADO el 2026-09-22: nuestro ensamblado SÍ carga y ejecuta en el sandbox.** Prueba de humo: un `POST` a `sanic_mppp_tbl_parametros` con `sanic_nombre` = `"ZZ Ensayo Mal Formado"` devolvió `400` con el texto exacto de nuestro código — *"La columna 'sanic_nombre' no tiene un valor válido: El nombre del parámetro solo admite letras, dígitos y puntos."* Eso prueba de punta a punta: paquete subido → `pluginassembly` cargado → `plugintype` resuelto → step `Create de Parametro` PreOperation disparado → `NormalizarYValidarStep` ejecutado → `InvalidPluginExecutionException` propagada a quien llama. No quedó ninguna fila basura (el alta se rechazó). **Falta todavía** lo de OpenXml y lo del serializador JSON: ese camino no los toca.

- **CERRADO el 2026-09-22 — prueba de humo de punta a punta, con la plantilla REAL del cliente.** `herramientas/prueba_humo_validacion.py` arma una copia de `datos/plantilla/Inclusiones_Exclusiones en PPP.xlsx` con dos filas ficticias, crea una Solicitud, le sube el Excel y llama `sanic_mppp_capi_validarsolicitud`. Resultado: `HTTP 200`, estado **En proceso**, 2 filas totales, 1 válida y 1 rechazada; acuse de 2673 caracteres; `sanic_versionparametros = estructura=1;listas=1;obligatoriedad=1`. Esto cierra de una vez **tres** de los ítems de abajo:
  - **OpenXml carga y corre en el sandbox** con los ensamblados que viajan en el paquete: leyó el Excel real.
  - **`DataContractJsonSerializer` bajo el .NET Framework del sandbox** leyó los cuatro parámetros JSON igual que bajo net10.
  - **`InitializeFileBlocksDownload`** bajó el archivo sin problema (queda pendiente solo el caso de la columna VACÍA).
  Y además valida lo que más importaba: **`plantilla.estructura` y `plantilla.listas` funcionan contra el Excel de verdad.** La fila buena quedó Validada con la referencia de formato 11 construida como `00700000000123456789` — `007` es el código real de BAC leído de la hoja `Listas`; con el código inventado del fixture de prueba (`102`) esa referencia habría salido mal y habría ido a AS400. La fila mala quedó rechazada con `sanic_banco` vacío (DD-01) y el mensaje `"Uno o más valores no son válidos: Banco."` (D-41, sin citar el valor).
  **Lo que la prueba destapó**: un defecto real de `CatalogosDataverse` que las 785 pruebas no veían, porque el doble en memoria era más permisivo que la plataforma. Ver `[[mppp/p09/bug-condicion-in]]`.

- **Qué ensamblados necesita OpenXml dentro del paquete (P-09, 2026-09-21).** El paquete de plugins quedó armado y contiene solo `Sanic.Mppp.Plugins.dll`, `DocumentFormat.OpenXml.dll` y `DocumentFormat.OpenXml.Framework.dll` (~7 MB): se excluyó el SDK de Dataverse, que ya vive en el sandbox. Antes de darlo por bueno hay que **leer un Excel real desde el plugin registrado en Dev**: si OpenXml 3.1.0 en .NET Framework pide en tiempo de ejecución algún ensamblado que se excluyó (System.IO.Packaging, System.Memory y compañía), el error recién aparece ahí. Es la misma prueba de humo que ya pide este apartado para los parámetros JSON.

- **El registro de los steps, contra la lista blanca (revisión de código, 2026-09-21).** La lista blanca de columnas (`04` §1) es el control que compensa que `W` sea sobre la fila entera, pero su eficacia depende de CÓMO queden registrados los steps, y eso no vive en el código.
  - **Hecho el 2026-09-22**: los dos steps quedaron registrados sobre `Update` de Fila y de Solicitud, PreOperation, orden 0, sin filtro de atributos, habilitados. Verificado leyendo `sdkmessageprocessingsteps` del entorno, no solo por la respuesta del alta.
  - **Cerrado el 2026-09-22, `Upsert` NO se puede registrar**: `Upsert` no aparece entre los `sdkmessagefilters` de `sanic_mppp_tbl_fila`, así que la plataforma no admite un step sobre ese mensaje para una tabla custom. Los mensajes que sí admite la tabla son: Archive, Assign, BulkArchive, BulkRetain, Create, CreateMultiple, CreateMultipleLegacy, Delete, DeleteMultiple, GrantAccess, IsValidStateTransition, ModifyAccess, PurgeArchivedContent, PurgeRetainedContent, Restore, Retain, Retrieve, RetrieveMultiple, RetrievePrincipalAccess, RetrieveSharedPrincipalsAndAccess, RevokeAccess, RollbackRetain, SetState, SetStateDynamicEntity, Update, UpdateMultiple, UpdateMultipleLegacy, ValidateArchiveConfig, ValidateRetentionConfig.
  - **CERRADO el 2026-09-22, `UpdateMultiple` / `CreateMultiple` / `Upsert` NO evitan el control.** Dataverse **funde** los pipelines: *"When you use a bulk operation message, the respective Create and Update event occurs for each Entity instance in the Targets parameter. Any plug-ins or other event handlers for the corresponding individual events continue to work as they always have."* (Learn, [Bulk operation messages](https://learn.microsoft.com/power-apps/developer/data-platform/bulk-operations#message-pipelines-merged)). Y para `Upsert` sobre una tabla **estándar**, la plataforma llama `Update` o `Create` según exista o no el registro, así que dispara sus steps (Learn, [Use Upsert](https://learn.microsoft.com/power-apps/developer/data-platform/use-upsert-insert-update-record#understand-the-upsert-process-for-standard-tables)); lo contrario solo vale para tablas **elásticas**, que no usamos. **No hacen falta steps adicionales: 8.2 queda en 18.**
  - **`Assign` está cerrado por privilegios**, no por el step: en `04` §1 el privilegio `As` (asignar) lo tiene únicamente el Administrador de planes, y solo sobre Cliente. Ni Ejecutivo ni Supervisor pueden reasignar una Fila o una Solicitud.
  - **RIESGO RESIDUAL ACEPTADO, `SetState` / `SetStateDynamicEntity`.** Desactivar una fila en Dataverse no tiene privilegio propio: alcanza con `W`, que el Ejecutivo y el Supervisor sí tienen sobre Fila y Solicitud. Un `Update` que traiga `statecode` SÍ pasa por la lista blanca y se rechaza; una llamada directa a `SetStateRequest` es otro mensaje y Learn **no dice** si dispara el step de `Update`. Registrar la lista blanca también sobre `SetState` **no serviría con el código actual**: ese mensaje no trae un `Target` de tipo `Entity` sino un `EntityMoniker`, así que `PlomeriaDePlataforma.Preparar` cortaría sin hacer nada. Alcance del daño si el agujero existe: una persona con `W` podría **desactivar** una Fila o una Solicitud — no puede cambiar ninguna columna de negocio, y queda en la auditoría nativa. **Se comprueba empíricamente en la prueba de humo de P-09** (intentar desactivar una Fila con un usuario Ejecutivo y ver si el step frena); si resulta que el agujero existe y molesta, se cierra con un step propio que lea `EntityMoniker`, no con la lista blanca.
  - **`CreateMultipleLegacy` / `UpdateMultipleLegacy`**: no están documentados en Learn. Son la plomería interna de los mensajes `*Multiple`, que ya funden con Create/Update, así que se asumen cubiertos; si la prueba de humo deja dudas, se verifican ahí.
  - **Pendiente**: comprobar que ninguna persona tenga `applicationid` en su usuario, porque esa marca exime del control (`BaseDeStep.EsCodigoDeServidor`).

- **`InitializeFileBlocksDownload` sobre una columna de archivo vacía (revisión de 7.6, 2026-09-21).** El código asume que la plataforma responde con una falla del servicio (que se propaga). Learn no lo dice. Comprobarlo en Dev en la prueba de humo de P-09 con una Solicitud sin Excel: si en vez de fallar devuelve tamaño 0, `ArchivosDataverse` ya lo trata como archivo vacío, pero hay que saber cuál de las dos pasa.

- **El acuse y la respuesta final en clientes de correo reales (revisión de 7.4, 2026-09-21).** El cuerpo es un documento HTML completo con estilos inline que Power Automate mete en un *Reply to email*. Antes de producción, enviar un acuse de prueba y mirarlo en Outlook de escritorio (motor de Word: bordes de tabla con `border-collapse`, apóstrofos), Outlook web y Gmail. Enmascarado: un carácter fuera del plano básico (emoji) en la cuenta podría cortarse a la mitad al enmascarar; riesgo aceptado, una cuenta bancaria no trae emojis.

- **Serializador JSON y `Regex` en el sandbox real (revisión de código, 2026-09-21).** Las pruebas corren en net10 contra el ensamblado net462: `DataContractJsonSerializer` se ejecuta con la implementación de net10, nunca con la de .NET Framework del sandbox. En el PRIMER despliegue del plugin (P-09) hacer una prueba de humo en Dev que cargue los parámetros reales (`plantilla.estructura`, `plantilla.listas`, `plantilla.obligatoriedad`, `lectura.limites`) y valide una solicitud de punta a punta. Si una forma del JSON se lee distinto, aparece ahí.

Todas hechas el 2026-09-20 y asentadas en `01-convenciones.md` §7. Queda una sola, que se cierra al construir el primer flujo: si Power Automate respeta la columna `uniquename` en un cloud flow.

## C. Spike C-05

**Cerrado.** Parte A y parte B terminadas; la condición C-05 de la definición está cumplida: Open XML SDK carga y lee la plantilla dentro del sandbox en milisegundos. Conclusiones en `spikes/c05-parseo-excel/CONCLUSIONES.md` y `CONCLUSIONES-B.md`. Quedó una sola cosa sin medir, porque necesita acceso al buzón: el largo real de los Message-ID que llegan (DD-03). Se mide al construir Flow A.
